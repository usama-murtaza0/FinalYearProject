<div class="py-3">
  <div class="container">
    <div class="row hidden-md-up">  
      <% @user.orders.each do |order| %>
        <div class="card-single-show col-md-6">
          <div class="card-body">
            <div class="card-block">
              <p>Name: <%= order.name %></p>
              <p>Email: <%= order.email %></p>
              <p>Line Items: </p>
              <% order.line_items.each do |item| %>
              <a href="<%= product_path(item.product.id) %>">
                <% if item.product.picture.present? %>
                  <img src="<%= item.product.picture %>" class="d-block user-select-none" width="100%" height="250" aria-label="Placeholder: Image cap" focusable="false" role="img" preserveAspectRatio="xMidYMid slice"></img>
                <% else %>
                  <%= image_tag "placeholder_order.png" , class: "placeholder-img" %>
                <% end %>
                <h3 class="card-header"><%= item.product.quantity %> x <%= item.product.title %></h3>
                <div class="card-body">
                  <h5 class="card-title"><%= item.product.price %> 
                  <label class="card-subtitle text-muted">Rs</h5></label>
                </div>
                <div class="card-body">
                  <p class="card-text"><small><%= truncate(item.product.description, :length => 20) %>...See More</small></p>
                </div>
              </a>
              <% end %>
            </div>
          </div>
        </div>
      <% end %>   
    </div>
  </div>
</div>

            
                    